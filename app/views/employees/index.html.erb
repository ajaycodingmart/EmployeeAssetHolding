<h1>This is the list of employees working</h1>
<ul>
	<%Employee.all.each do |emp|%>
	<li>Name :<%=emp.name%></li>
	<li>Company Name :<%=emp.company.name%></li>
	<p>Check <%=emp.name%>'s details <%= link_to 'Show', employee_path(emp)%></p>
	<br>
	<%end%>
</ul>
<p>Total Number of employees avaliable are <%=Employee.all.count%> </p>
<%=link_to 'Back to Main Index', company_index_path%>
